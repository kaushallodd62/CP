#include<stdio.h>
int main()
{
 int no_b,no_l,no_d;
 FILE *fp;
 fp=fopen("","r");
 fscanf("%d%d%d",&no_b,&no_l,&no_d);
char str[no_b];
for(i=0;i<str[i]!="\0";i++)
 {
  fgets(str[i],fp)
 }
 struct l
 {
  int no_books,sgpd,maxship;
  char b_ids[no_books]
 }
 for(i=0;i<no_l;i++)
 {
   struct l i;
   fscanf("%d%d%d",&i.no_books,&i.sgpd,&i.maxship);
   fgets(i.b_ids,fp)
  }
  	
}
